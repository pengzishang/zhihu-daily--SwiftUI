# 2026-08-08

- 按项目约定清理远端分支：`origin/feature/ai-quick-prompts` 已完整合入 `main`，安全删除。保留 `main`（已整合主线）与 `antigravity/daily-news-dev`（远端默认分支）。两个 `copilot/fix-github-actions-build*` 分支未合入 `main`，按"不得强制删除未合并分支"约定保留，待子上确认是否确实废弃后再决定是否强删。

- 修复 version/2.1 分支编译警告（提交 `f4a4789`）：① ArticleDetailView.swift 的 `ReadQualificationTimer.requiredViewingDuration` 加 `nonisolated`（Swift 6 主线程隔离警告：main-actor 静态属性被非隔离上下文的默认参数引用）；② Info.plist 加 `UIRequiresFullScreen=true`（消除 All interface orientations 警告）；③ project.pbxproj 链接系统 `AppIntents.framework`（消除 Metadata extraction skipped 警告）。**注意**：找警告的来源是 DerivedData 里的 xcactivitylog（gunzip 解压后 grep "warning:"），因为本沙箱会拦截 xcodebuild 内部的 sandbox-exec 导致 SPM 包解析失败、无法真正编译；用 swiftc 直接对 app 源码做 Swift 6 类型检查会因依赖的是 Swift 5.9 预编译的 Alamofire/Kingfisher 模块而产生假的 Sendable 报错，不可作为验证依据。子上需在正式 Xcode 环境 `xcodebuild build -scheme DailyReader` 回归确认警告清零。

- 修复文章列表左滑手势冲突：`HomeView.swift` 的 `SwipeToHideContainer` 之前用 `.simultaneousGesture(DragGesture)` 包在 `NavigationLink` 内容上，左滑松手会同时触发进入文章。改为在外层 `ZStack`（`.clipped()` 之后）用 `.highPriorityGesture`，并新增 `@State dragging` + 内容 `.allowsHitTesting(!dragging)`：滑动时禁用链接点击，松手恢复，普通点击不受影响。`xcodebuild build` 通过（BUILD SUCCEEDED）。

- 研究"文章正文解析成原生格式"可行性（子上要求先不写代码）。现状：100% 走 WKWebView（`HTMLWebView`，body 为原始 HTML，loadHTMLString + 注入 CSS/JS 测高），零 HTML 解析层，三方依赖仅 Alamofire+Kingfisher，设计文档定调"单一 WKWebView、不新增依赖"。拉取 4 篇真实日报正文统计标签，集中在 p/img/div/figure/a/strong/span/figcaption/br/hr/blockquote/em/sup/h2/script，均少量规整；另有 div.meta 作者块、RichText-LinkCard 富链接卡、data-original 高清图源。热榜回答(mock answers_complex_html)含 LaTeX `$$` 与 `<pre><code>`。结论：路线 B（HTML→内容块模型→原生 SwiftUI）可行，解析器约覆盖 15 标签+2 结构块，需新建 ~6 原生组件（段落/图文/引用/作者块/LinkCard/代码块），图片复用 Kingfisher；因 WebView 不执行 js，LaTeX 当前也是裸显示，原生无回归。待子上决定是否进入方案设计。

- 需求设计讨论（grill-me）：「文章分类 + 兴趣指数」功能，5 支决策已定，待进入实现。
  - 分类体系：固定类目；由 AI 对真实文章样本归纳 8–12 个不重叠大类后**冻结**；必有"其他"兜底；冻结前弹"你的阅读类目"页让用户删/并/改名。背景：知乎日报接口无每篇分类字段，`/api/4/themes` 现已返回空，无法抓取官方栏目。
  - AI 分类实现：进文章（详情加载/刷新）时后台异步调已配置的远程 AI 分类并本地缓存；离线/无 Key 走本地关键词兜底或归"其他"；AI 返回不在集合内或低置信度一律归"其他"。
  - 兴趣信号采集：复用现有 `ArticleScrollObserver` 的 `readingProgress`（滑动%）与 `ReadQualificationTimer` 的前台活跃停留统计；每篇存累计汇总（累计停留秒数、最大滑动%、阅读次数、最近阅读时间）；收藏=强正、不感兴趣(hide)=强负；纯本地不外传。
  - 兴趣指数算法：单篇 score=clamp(0.4×滑动%+0.4×停留归一+0.2×阅读次数)，收藏额外强正、不感兴趣≈0（停留用封顶法，如 ≥180s 满格）；类目=成员文章得分均值，成员<3 篇标"样本不足"；轻度时间衰减（半衰期≈30 天，按最近阅读时间）。
  - 展示：「我的」页（MeView）阅读档案卡下方新增"兴趣画像"卡，横向条形榜按指数降序列各类目（含百分比，<3 篇标样本不足，"其他"置底弱化）；并在首页/收藏列表的每条文章行加一个 AI 类目标签。

- 实现完成（grill-me 设计 → 标准 SOP 团队落地）：git worktree `/Users/pengzishang/Current Project/category-interest`（分支 `feature/category-interest`，基于 version/2.1）按 PM→架构→工程→QA 跑通。交付：PRD（docs/features/category-interest-prd.md）、设计+任务分解 T1–T9（category-interest-design.md + class/sequence mermaid）、工程师实现提交 `5cc49e9`（22 新文件/7 改：纯函数 InterestIndexEngine、复用 OpenAICompatibleChatService 的后台分类、Application Support JSON 存储 actor、MeView 兴趣画像卡、StoryRow 类目标签、CategoryOnboardingView 冻结页、XcodeGen project.pbxproj 已重新生成纳入新文件）、QA 修复置信度阈值 `static let`（`0b89e72`）+ 补 10 类边界用例（`050c628`）。**环境限制**：本沙箱禁止 xcodebuild 内部 sandbox-exec 做 SPM 包解析（Alamofire/Kingfisher），真实编译/单测未能跑通，需在具备网络/SwiftPM 权限的 Xcode 环境回归 `xcodebuild build/test -scheme DailyReader`。主树 version/2.1 未动（仍 27dc0e0）。未合并。

- 合入 `version/2.1`：因远程无 `version/2.1` 分支，"发 PR → 2.1"以本地合入实现。`feature/category-interest`（HEAD `050c628`）通过 merge commit `0e83c6a` 合入 `version/2.1`（no-ff，因主树先提交了 memory 归档 commit `e006218` 导致分叉，非纯 FF）。feature 分支保留（未合入 main，按约定不删）。文件统计：29 文件 / +3116 行，含 22 新 + 7 改（含重生成 pbxproj）。主树工作区仍有一份未跟踪残留 `outputs/文章分类与兴趣指数-设计.md`（来源不明，未动）。

- 路线 B 实现（原生正文渲染）：worktree `/Users/pengzishang/Current Project/native-body-render`（分支 `feature/native-body-render`，基于 27dc0e0）。新增 `DailyReader/Features/Detail/NativeBody/`：模型 `ArticleBlock`(indirect enum) + `HTMLToBlocksParser`(容错分词器+递归下降) + 10 个原生视图 + `NativeBodyRenderer`(带缓存，解析失败/空回退 WebView) + `FeatureFlag.useNativeBody`(@AppStorage 默认 false)。接入 `ArticleDetailView`/`HotListView` 双轨（flag=false→WebView 零回归；=true→原生），`SettingsView` 加开关。`xcodebuild build` 通过。修复两个解析器关键 bug：(1) `parseAuthorMeta` 的 `case .close("div"): break` 只跳出 switch 没跳出 while —— 改为 `metaLoop:` 标签 + `break metaLoop`，否则 authorMeta 命中 `</div>` 后仍把后续所有 token(含 LinkCard/code/script)吞到 EOF；(2) LinkCard 标题嵌套在 `LinkCard-contents` 内，原逻辑误把 contents 当 description、title 退化为 host 兜底 —— 新增 `extractLinkCardText` 递归提取 `LinkCard-title`/`LinkCard-desc`。新增 `DailyReaderTests/NativeBody/NativeBodyParserTests.swift`(全量块序列/两个回归/实体解码/脏 HTML 容错/纯文本展平/空输入)。已提交 `7ff7177`（build 通过，NativeBodyParserTests 7 项全过；其余测试受 Xcode 27 beta 沙箱限制未能完整跑通，待正式 Xcode 环境回归 `xcodebuild test -scheme DailyReader`）。

- 「我的」页整体滚动改造（提交 `aa27adc`，version/2.1）：将 MeView 从"顶部固定 VStack + 下方独立 List"改为单一可滚动结构——顶部刊头/阅读档案/兴趣画像卡/分段控件/搜索框打包成 `topSection`（作为首行 `Section`），分别注入到「收藏」「已读」两个 `List` 的首位（`List` 配 `.scrollContentBackground(.hidden)`），这样整页随列表一起滚动；`body` 用 `ZStack { DS.paper.ignoresSafeArea(); contentList.frame(maxWidth:720) }` 并 `.toolbar(.hidden, for: .navigationBar)`，保留 UI 测试标识符（me.header/me.readingArchive/me.favorites.list/me.read.list/me.segment.read/me.searchField）。保留两个独立 List 以兼容 MeFlowUITests 的 `waitForExistence` 自动滚动定位。

- 修复「我的」页顶部区块误触进入设置 + 分段无法切换（提交 `21c5c39`，amend 自 `a83921f`，version/2.1）：根因是「整体滚动改造」把设置入口的 `NavigationLink` 放进了 `List` 首行 `Section`——SwiftUI 会让该**整行**变成导航入口，点「收藏/已读/搜索」等同行元素都会误进设置，且会吞掉分段按钮的点击（切不了分段）。初版用 `.navigationDestination(isPresented:)` 仍属 List 上下文、不够稳，故改用更稳写法：齿轮改为纯 `Button`（置位 `@State showSettings`）；设置页改用**放在 `List` 之外的隐藏 `NavigationLink(destination:isActive:$showSettings) { EmptyView() }`** 推入（iOS 17 目标下 `isActive:` 未弃用、无警告）。列表首行从此不含任何 `NavigationLink`，仅齿轮进设置，分段切换恢复。注意：本沙箱仍无法 `xcodebuild`（sandbox-exec 拦截 SPM 解析），需子在正式 Xcode **Clean Build** 后点按回归（旧包 aa27adc 仍带此 bug，若未重编会误以为没修好）。UI 测试约束：`MeFlowUITests` 用 `collectionViews["me.favorites.list"]`/`["me.read.list"]`（SwiftUI List→collectionView）、`me.settingsButton` 推入后断言 `navigationBars["设置"]`——本修法均满足。

- 「我的」页再次重构（提交 `9eba9c6`，version/2.1）：子上在 `21c5c39` 之后仍报"点收藏/已读/统计表都进设置 + 分段切不了"，但 `21c5c39` 源码中顶部区块已无指向设置的 `NavigationLink`（齿轮是纯 Button），该症状与更早的 `aa27adc`（齿轮确为 List 行内 NavigationLink）完全吻合——强烈提示**子本机跑的是未重编的旧包**。为彻底消除"List 行内嵌交互区块"这一整类隐患，本次把 `topSection` 从 `List` 首行 `Section` **整体移出**，改为放在外层 `ScrollView > VStack` 里的独立 `VStack`（纯布局、零 List 行手势）；两个 `List` 仅渲染内容行并 `.scrollDisabled(true)`，由外层 `ScrollView` 统一滚动（仍满足「整页滚动」需求），且保留 `List` 与 `me.favorites.list`/`me.read.list` 标识符（UI 测试不受影响）；设置入口仍是 ZStack 内、List 之外的隐藏 `NavigationLink(isActive:)` 推入。结论/教训：**把含 Button 的交互区块塞进 `List` 行是 SwiftUI 反模式**（整行变导航入口 / 吞按钮点击），应置于普通 `VStack`；且本沙箱不能实编，每次改 UI 后必须提醒子在正式 Xcode **Clean Build**（删 DerivedData 该项目或 Product→Clean Build Folder），否则极易误判"没修好"。

- 修复首页文章全部消失（提交 `accae7d`，version/2.1）：子 Clean Build 后首页只剩"今日故事"开场卡、其余空白，横幅"刷新失败，已保留上次内容"，且「我的」收藏/已读皆空。根因是 **HomeViewModel 阅读状态错位**：`readStoryIDs`（首页 `visibleSections` 用它判断隐藏）与 `readStories`（「已读」列表数据源）是**两个独立存储**，分别从 UserDefaults / Keychain 两条通道在 init 恢复，可能错位产生"无明细的幽灵已读 ID"——首页据此把文章全当已读隐藏，而「已读」列表因 `readStories` 为空显示空白（`topStories` 开局卡不受 `visibleSections` 过滤故仍显示）。应用层 `markStoryRead(_:date:)` 本就同步两者（自动标已读 ArticleDetailView:381、HotListView:476 均调两参版），故非代码调用bug，而是恢复通道错位。修复：init 末尾以 `readStories` 为准 `Set(readStories.map{$0.id})` 校正 `readStoryIDs` 并回写 UD+Keychain。**关键架构事实**：`AppRootView.makeService()` 正常运行走 `ZhihuDailyService`（真实接口+磁盘缓存 `caches/DailyReaderCache`），仅 `-UITestMode` 才用 `LocalFixtureDailyService` 且会清空缓存；Clean Build 不删缓存，删 App/重置模拟器才会；首页链路（HomeView/HomeViewModel/DailyRepository/DiskCacheStore/首页模型）近期合并均未改动。

- 冷宫（hiddenStories）排查 + 一键恢复（提交 `27b8fdf`，version/2.1）：子 re-run `accae7d` 后仍报首页空、且发现「设置→冷宫」里能看到那些文章。逐行核对后确认**数据未丢，是被标记为「不感兴趣」移入冷宫**。关键事实：`visibleSections`/`favoriteSections`/`readSections`/`visibleReadStories` 全部遵循 `!isStoryHidden && !isStoryFavorited && !isStoryRead` 的统一过滤——一篇文章进了 `hiddenStories`，就会被首页/收藏/已读三处同时排除，唯独冷宫专门展示，因此"首页空+收藏已读列表空+冷宫有文章"完全自洽。`hideStory` 仅由 HomeView/HotListView 左滑 `SwipeToHideContainer`（需滑过 -118pt 才触发，不会批量/误触）与 ArticleDetailView 菜单调用；`hiddenStories` 是单一数据源、无任何加载时自动隐藏路径，跨构建由 UD+Keychain 持久化——故基本排除"代码 bug 把整页藏了"，应是更早前被「不感兴趣」移入、一直留存。修复：新增 `HomeViewModel.restoreAllHidden()`（清空并持久化）+ `ColdPalaceView` 右上角「恢复全部」按钮，可一键救回。子确认要加二次确认防误触。

- 「不感兴趣」加二次确认（提交 `3851909`，version/2.1）：子选"加二次确认"。三处隐藏入口全部改为先弹 `confirmationDialog`（标题"不再看这篇/这条热榜？"，含"会被移入冷宫，之后可在「设置→冷宫」恢复"提示），确认后才真正 `hideStory`，取消则不动：① HomeView `SwipeToHideContainer`——左滑露出按钮点按、**以及滑过 -118pt 自动隐藏**两条路径都改走 `confirmHide` 状态（不再直接 `performHide`）；② HotListView 热榜 `.swipeActions` 改存 `pendingHideSummary` 再弹确认；③ ArticleDetailView 菜单「不感兴趣」改置 `confirmHide`。冷宫恢复能力保留（逐条"恢复" + 右上角"恢复全部"）。

- **关键验证（子自测，晚）**：子在**另一台全新模拟器**编译运行，首页/收藏/已读**全部正常**；而手机上仍空、冷宫有文章。**这彻底坐实诊断**：文章未丢、代码无误，是**手机本机持久化状态（`hiddenStories` 冷宫 + 可能 `readStoryIDs`）**把文章过滤掉了——这些状态存于手机 `UserDefaults`+`Keychain`，不随换模拟器/重编而消失，故干净模拟器无此状态、一切正常。手机恢复路径：① 更新手机到最新构建（含 冷宫「恢复全部」）→ 设置→冷宫→恢复全部（只清隐藏，保收藏/已读）；② 老包也能进冷宫逐条左滑"恢复"；③ 删 App 重装（清空全部本地状态，但收藏/已读记录也清零）。**教训**：跨设备/模拟器的"同一份代码表现不同"，首选怀疑本机持久化状态差异，而非代码 bug——这次干净模拟器=正常就是铁证。

- 子明确**拒绝**「设置→重置全部本地阅读状态」按钮：因"用了很久、读了很多文章"，不愿清掉收藏/已读记录——故不再做该按钮。手机恢复坚持走「冷宫→恢复全部」（仅清 hiddenStories，保 read/favorite）。**已提示设计事实**：首页按 `!isStoryRead` 自动隐藏已读文章，重度使用手机自然显得空；新模拟器零读零藏故全显。子暂未决定要不要让"已读文章仍留首页"，列为潜在后续需求（待其拍板）。

- **新增「阅读状态备份 / 恢复」功能**（version/2.1，未提交时由子要求导出已读/收藏/冷宫状态以便重装后导入）：经 software 团队快速模式（Eng 实现 + QA 测试）。实现：① `HomeViewModel` 新增 `exportState() throws -> Data`（组装 `ReadingStateBackup` 结构体 `schemaVersion/appVersion/exportDate/readStoryIDs[Int]/hiddenStories/favoriteStories/readStories`，`JSONEncoder(.prettyPrinted,.sortedKeys)`）+ `importState(_:)`（解码→schema 版本校验(>`backupSchemaVersion` 抛 `unsupportedSchema`)→**并集**合并：`readStoryIDs.formUnion`、明细经私有泛型 `mergeStories<T:Identifiable&Equatable,ID==Int>` 按 id 去重「现有优先」→ 调 `saveReadStoryIDs()/saveHiddenStories()/saveFavoriteStories()/saveReadStories()` 全量双写 UD+Keychain）；新增 `saveReadStoryIDs()` 抽离既有的 readStoryIDs 双写逻辑，重构 `markStoryRead/toggleRead` 复用之；`ReadingStateBackup`/`ReadingStateImportError(LocalizedError 中文)` 放在 HomeViewModel.swift 文件底部（**刻意不新建文件**，因 XcodeGen 沙箱跑不了 `xcodegen` 重新生成 pbxproj，新文件不进 pbxproj 会导致 Cannot find type）。② 编译版 `Features/Home/SettingsView.swift`（**注意** project.yml `excludes: [Features/Settings/SettingsView.swift]`，真正编译的是 Home 那份）「阅读管理」Section 新增「导出阅读状态」(`performExport` 写 `temporaryDirectory/DailyReader-State-Backup-yyyyMMdd-HHmm.json` + `.sheet(ShareSheet(items:[url as Any]))`) 与「导入阅读状态」(`.fileImporter([.json])` + 安全范围资源读 Data + `handleImport` 弹 `已恢复 N 条已读/M 条收藏/K 条冷宫`)；被 exclude 的 `Features/Settings/SettingsView.swift` 也同步改了（dead 副本，不影响编译）。③ QA 新增 `DailyReaderTests/ReadingStateBackupTests.swift`（12 项：序列化往返含 ReadStory 自定义解码/缺 readAt 兜底/损坏 JSON 抛 `.invalidFile`/schema 拒绝/空集整体恢复/现有优先合并/端到端 export→import/merge 算法等价复刻），用 `StubHomeRepository`+`InMemoryReadingStateBackup` 真实构造 HomeViewModel 跑 importState。沙箱不能 xcodebuild，源码经只读审查判 NoOne（无 Bug）；QA 提一处非阻塞观察：importState 未对 readStoryIDs 与 readStories 联动校正幽灵 ID，但下次启动 init 的 reconciliation 会自纠。待子在正式 Xcode Clean Build 回归 + 跑测试。
