# 2026-08-11

## 修复首页历史分页 footer 不触发加载

- 问题：`HistoryPaginationFooter` 在 `.idle` 状态下只有 1pt 高度且未监听 `onAppear`，用户手动上拉到底部无法触发 `loadMore()`。
- 改动：`DailyReader/Features/Home/HomeView.swift` 的 `HistoryPaginationFooter` 增加 `.onAppear`，仅在 `.idle` 时兜底调用 `loadMore()`。
- 验证：`swiftc -parse` 语法检查通过；因沙箱 SPM resolve 受限，未跑完整 xcodebuild，但改动仅影响 View 层 modifier，逻辑简单。
- 提交：`c53a3c9` on `version/2.1`。
