# 2026-08-17

- **文章详情载入动画**：子要求在详情页加载时加入载入动画，指定走独立分支 `feature/detail-loading-animation`（基于 `version/2.1`）。
  - 先出 3 套备选（预览页 `outputs/detail-loading-animations.html`）：A 印章呼吸、B 纸稿骨架、C 墨线书写。
  - 子上选定 **A+C 混合**：正文区用朱砂「知」字方章呼吸（品牌）+ 墨线落笔写出 + 朱砂墨点随锋（动感）；刊头（封面/标题/文武线）沿用列表数据即时渲染不动。
  - 落地：新增 `DailyReader/Features/Detail/ArticleDetailLoadingView.swift`，并在 `ArticleDetailView` 的 `.loading` 分支替换原系统 `ProgressView`。全程复用 `DS` 设计系统动态色，读取 `accessibilityReduceMotion` 做静态兜底。
  - 待验证：沙箱无法 SPM resolve，需子本地 `xcodegen generate` 后 build/run 实机确认动效。
  - **修复编译错误**：`.offset(x:)` 三元分支 `geo.size.width - 6.5 : -6.5` 因 `CGFloat` 与 `Double` 字面量无法隐式统一报错，已改为显式 `CGFloat(6.5)` 包装。文件其余无编译问题。
  - **本地编译验证（沙箱）**：xcodebuild 内部 `sandbox-exec` 在受限环境被拒，需绕过——`SWIFTPM_DISABLE_SANDBOX=1 SWIFT_BUILD_USE_SANDBOX=0` + `-IDEPackageSupportDisableManifestSandbox=1 -IDEPackageSupportDisablePackageSandbox=1` + `OTHER_SWIFT_FLAGS='$(inherited) -disable-sandbox'`；并加 `CODE_SIGNING_ALLOWED=NO` 跳过签名（避免 login.keychain 沙箱拦截）。Xcode 27 / swift 6.4 下 `xcodegen generate` 后 clean build 通过，`ArticleDetailLoadingView.swift` 已纳入编译、0 error。子上本地"仍然失败"多半是未跑 `xcodegen generate` 致新文件不在 target（报 `Cannot find type 'ArticleDetailLoadingView' in scope`），与代码无关。
