# 2026-08-03

- 在基线 `ea9a32a` 上创建 `review/code-audit-20260803`，完成全仓代码审查并产出 `outputs/代码审查报告-2026-08-03.md`。
- 确认 6 项问题：内置 AI Key 被编译进 Info.plist（P1）、AI 会话旧快照覆盖新状态竞争（P1）、首页 Feed stream 不传播取消（P2）、通用 Keychain helper 缺少 service/错误处理（P2）、OAuth 生产代码与全部相关测试被 target 排除（P2）、共享 JSONDecoder 并发契约不明确（P3）。
- AI 交叉审查的 ChatGPT 浏览器通道两次断连，Gemini 缺少 Cookie，API 缺少 Key；未产生可采纳的第二模型结论。完整 xcodebuild 也因执行环境的 `sandbox-exec: sandbox_apply: Operation not permitted` 阻塞，报告中已明确限制。
- 根据用户决定，将 P1-1 明确排除，完成 P1-2、P2-1、P2-2、P2-3、P3-1 的分阶段修复计划，文档为 `outputs/代码问题修复计划-2026-08-03.md`。关键决策：会话持久化用 actor 内 revision 防倒退；Feed 用 onTermination 传播取消；Keychain 使用 service/account、throwing API 与复制验证后精确删除的迁移；HTTPClient 每请求创建 decoder；OAuth 纳入默认编译和测试，但普通运行固定 unavailable，仅 `-UITestMode` 使用 fixture，不配置或启用真实生产登录。
- 在 `fix/code-audit-remediation-20260803` 完成除 P1-1 外五项修复：AI session revision 防旧快照覆盖；Feed stream 终止取消 producer；Keychain 按 reading/AI/OAuth 三个 service 隔离并安全迁移旧项，删除旧 helper；HTTPClient 每请求独立 decoder；OAuth 源码/单测/UI 测试纳入默认 targets，普通启动强制 unavailable，只有 `-UITestMode` 使用 fixture。XcodeGen 连续生成成功、修改 Swift 文件均通过语法解析、P1-1 非目标文件无变化；完整 xcodebuild 即使在命令沙箱外仍被 SwiftPM 的 `sandbox-exec: sandbox_apply: Operation not permitted` 阻塞，不能声明构建或测试通过。
- 完成修复任务最终收尾：分支工作区干净，HEAD 为 `ba9998f`，修复实施与交付文档均已提交；回归任务按“静态门禁通过、完整构建与 XCTest 受环境阻塞待正常 Xcode 环境补跑”关闭。
- 用户反馈当前编译失败后，在新分支 `fix/compile-errors-20260803` 从已有 Xcode 编译日志定位到 `HomeViewModel.init` 在四个状态属性初始化前调用实例 Keychain 方法。改为初始化器局部闭包直接使用注入的 `ReadingStateBackingUp`，消除提前使用 `self`；同时将 `AIConfigurationStore` 两个纯常量 ID 标记为 `nonisolated static let`，消除 Swift 6 actor 隔离升级风险。定向 `swiftc -typecheck` 已通过，XcodeGen 和 diff 检查通过；完整 xcodebuild 仍被环境的 SwiftPM `sandbox-exec` 阻塞。源码修复提交为 `af17790`，补充交付文件 `outputs/编译问题修复概览-2026-08-03.md`。
- 在分支 `feat/read-after-10-seconds-20260803` 将自动已读规则改为日报来源的未读文章在详情前台有效停留累计满 10 秒后触发；收藏、冷宫、已读列表明确不参与自动已读，热榜回答详情仍独立使用 10 秒规则。移除各入口的立即已读逻辑；切后台暂停计时，同一文章回前台继续累计，不同文章不共享时长；详情菜单统一为仅在已读时可用的“设为未读”。补充计时与来源资格测试，所有修改文件通过 `swiftc -parse` 与 `git diff --check`；完整 xcodebuild 仍在 SwiftPM 解析阶段被 Xcode 27 Beta 的 `sandbox-exec` 权限错误阻塞。主要提交 `9d916f0`、来源修正提交 `af66b69`。
- 将已读功能分支以合并提交 `46702fd` 合入本地 `main`，随后合入 `origin/main` 独有的 CI 工作流提交，最终本地 `main` 同时包含产品改动与远程主线历史。基于最新 `main` 建立长期工作分支 `version/2.1`；重新校验后删除 10 个已经完整合入 `main` 的旧本地分支，未删除远程分支。后续默认在 `version/2.1` 持续提交，除非用户明确要求换分支。
